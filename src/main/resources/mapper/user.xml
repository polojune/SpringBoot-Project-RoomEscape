<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cos.roomescape.repository.UserRepository">
  <insert id="save">
    INSERT INTO user(username,password,email,role,createDate)
    VALUES(#{username},#{password},#{email},#{role},now())
  </insert>
  
  <select id="login" resultType="com.cos.roomescape.model.User"> 
     SELECT id,username,email,role,createDate FROM user WHERE username=#{username} 
      AND password = #{password} 
  
  </select>
  
  <select id="findByProviderAndProviderId" resultType="com.cos.roomescape.model.User">
      SELECT id,username,email,role,provider,providerId,createDate FROM user WHERE provider=#{provider}
      AND providerId = #{providerId} AND password = #{password}
       
  </select>
   
  
</mapper>